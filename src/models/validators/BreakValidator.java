package models.validators;
/*
import java.util.ArrayList;
import java.util.List;

import models.Break;

public class BreakValidator {
    public static List<String> validate(Break b) {
        List<String> errors = new ArrayList<String>();


        String work_date_error = _validateWork_date(b.getWork_date());
        if(!work_date_error.equals("")) {
            errors.add(work_date_error);
        }

        return errors;
    }


    private static String _validateWork_date(Date work_date) {
        if(work_date == null || work_date.equals("")) {
            return "日付入力してください";
            }

        return "";
    }
}
*/